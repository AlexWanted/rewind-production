---
phase: 01-ftp-backed-media-serving
plan: 03
type: execute
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified:
  - app/api/files/route.ts
autonomous: true
requirements:
  - MED-01
user_setup: []

must_haves:
  truths:
    - "Admin panel file listing comes from FTP as primary source"
    - "On FTP error, transparently falls back to local filesystem listing"
    - "Response format unchanged: { files: Array<{name, path, size, createdAt}> }"
    - "DELETE endpoint unchanged (still deletes from local only - per requirements, upload handles FTP)"
  artifacts:
    - path: "app/api/files/route.ts"
      provides: "GET handler listing files from FTP with local fallback"
      exports: ["GET", "DELETE"]
      contains: ["listFilesFromFTP", "readDirRecursive"]
  key_links:
    - from: "app/api/files/route.ts"
      to: "lib/ftp.ts"
      via: "import { listFilesFromFTP } from '@/lib/ftp'"
      pattern: "import.*listFilesFromFTP"
    - from: "app/api/files/route.ts"
      to: "public/uploads/"
      via: "fallback to local readDirRecursive"
      pattern: "readDirRecursive.*public.*uploads"
---

<objective>
Update /api/files GET to list files from FTP as primary source with transparent local fallback.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@app/api/files/route.ts
@lib/ftp.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GET handler to list from FTP first, fallback to local</name>
  <files>app/api/files/route.ts</files>
  <read_first>
    - app/api/files/route.ts
    - lib/ftp.ts
  </read_first>
  <action>
Modify app/api/files/route.ts:

1. Add import: `import { listFilesFromFTP } from '@/lib/ftp';`

2. In the GET handler, wrap the FTP listing in try/catch:
   - Call `listFilesFromFTP('')` for root listing (FTP_BASE_PATH is prepended inside)
   - On success: return NextResponse.json({ files }) with FTP results

3. In catch block: implement fallback to local filesystem:
   - Use existing `readDirRecursive(join(process.cwd(), 'public', 'uploads'), '')`
   - Return same response format

4. Preserve existing DELETE handler unchanged (lines 51-72) - it deletes from local only. Per requirements: "uploading files through /api/upload works as before" - upload already handles FTP. DELETE is not mentioned in requirements, keep as-is.

5. Error handling: distinguish between FTP connection error (fallback) vs other errors.

Key code structure:
```typescript
export async function GET(request: NextRequest) {
  try {
    const files = await listFilesFromFTP('');
    return NextResponse.json({ files });
  } catch (ftpError) {
    // Fallback to local
    const uploadsDir = join(process.cwd(), 'public', 'uploads');
    if (!existsSync(uploadsDir)) {
      return NextResponse.json({ files: [] });
    }
    const files = await readDirRecursive(uploadsDir, '');
    return NextResponse.json({ files });
  }
}
```

The listFilesFromFTP function already returns the same structure as readDirRecursive: {name, path, size, createdAt}. Ensure path format matches: `/uploads/...` (as produced by local version line 23: `path: /uploads/${relativePath.replace(/\\/g, '/')}`).

If FTP returns different path format, normalize it in listFilesFromFTP or in the GET handler.
</action>
  <verify>
    <automated>grep -q "import.*listFilesFromFTP.*from.*lib/ftp" app/api/files/route.ts && grep -q "listFilesFromFTP" app/api/files/route.ts && grep -q "readDirRecursive.*public.*uploads" app/api/files/route.ts && echo "FTP import, call, and fallback present"</automated>
  </verify>
  <done>GET /api/files lists from FTP first, falls back to local on FTP error, response format unchanged, DELETE handler untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/files | Untrusted query params (none currently used in GET), potential for parameter injection if added |
| Application → FTP Server | External service, plain FTP, directory listing exposes file structure |
| Application → Local FS | Trusted fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | GET /api/files | mitigate | FTP listing only under FTP_BASE_PATH; ensure no parent directory traversal; same validation as file retrieval |
| T-01-11 | Tampering | FTP directory structure | accept | Read-only listing; FTP server controls actual permissions |
| T-01-12 | Denial of Service | GET /api/files | mitigate | Add timeout on FTP list operation (30s); limit recursion depth (max 10 levels); cache results briefly (10s) if needed |

</threat_model>

<verification>
- GET /api/files returns file list from FTP when available
- GET /api/files falls back to local when FTP unavailable
- Response format identical: { files: [{name, path, size, createdAt}] }
- Path format consistent: `/uploads/folder/file.jpg`
- DELETE /api/files?path=/uploads/test.jpg still works (local only, unchanged)
- No regression in admin panel file listing
</verification>

<success_criteria>
- Admin panel shows files from FTP
- Fallback works when FTP server stopped
- File listing format matches existing frontend expectations
- No TypeScript errors
- DELETE endpoint unchanged
</success_criteria>

<output>
After completion, create .planning/phases/01-ftp-backed-media-serving/01-03-SUMMARY.md
</output>